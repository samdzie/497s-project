class HomepageDatabase:
    def __init__(self):
        """Database constructor."""

    def add_group(self, group_id):
        """Initialize a new group's homepage given the group's ID."""

    def edit_welcome(self, group_id, text):
        """Edit a group's welcome message given its group ID."""

    def edit_about(self, group_id, text):
        """Edit a group's about message given its group ID."""

    def edit_contact_info(self, group_id, text):
        """Edit a group's contact info given its group ID."""
